# Minimal dependency set for LEXCART_LITE=1 — the agentic-commerce surface
# only (auth, lawyers, Razorpay payments, concierge, AI-buyer API,
# campaigns). No LLM/RAG/ML/OCR stack; the concierge runs its deterministic
# parser. Used by the Docker evaluation image (see /Dockerfile).

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

sqlmodel>=0.0.22
psycopg[binary]>=3.2
pgvector>=0.3.6

PyJWT>=2.8.0
bcrypt>=4.1.0

razorpay>=1.4.2
httpx>=0.27.0
cachetools>=5.3.0
