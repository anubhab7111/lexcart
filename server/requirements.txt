# LangGraph and LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-ollama>=0.2.0
langchain-community>=0.3.0

# Vector store and embeddings
faiss-gpu
sentence-transformers>=2.0.0

# FastAPI backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Document processing
pypdf>=4.0.0
python-docx>=1.1.0
pdfplumber>=0.11.0
poppler-utils

# OCR for images and scanned PDFs
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.16.3

# Web scraping for lawyer search
httpx>=0.27.0
beautifulsoup4>=4.12.0
aiohttp>=3.9.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async support
aiofiles>=24.1.0

# Caching for performance
cachetools>=5.5.0

# In-process job scheduler (hearing reminders, cause-list polling, notifications)
APScheduler>=3.10,<4.0

# S3-compatible object storage client (Legal Document Vault -> Cloudflare R2)
boto3>=1.34.0

# Database (PostgreSQL)
sqlmodel>=0.0.22
psycopg[binary]>=3.2
pgvector>=0.3.6

# Auth
PyJWT>=2.8.0
bcrypt>=4.1.0

# Payments (Razorpay test mode)
razorpay>=1.4.2

# Hybrid retrieval (BM25 sparse index; reranker comes from sentence-transformers)
rank-bm25>=0.2.2

# Multilingual support: fastText language ID + IndicTrans2 translation.
# fasttext-wheel: fastText lid.176 language detector. NOTE: on very new
#   toolchains (GCC 13+, Python 3.14) the sdist needs `#include <cstdint>` added
#   to its sources to compile; see docs/multilingual.md.
# ctranslate2: runs the IndicTrans2 models. We deliberately do NOT use the
#   HuggingFace transformers modeling path — IndicTrans2's trust_remote_code is
#   written for transformers ~4.x and breaks on the transformers 5.x this stack
#   needs (sentence-transformers / BGE-M3 / reranker). CTranslate2 needs no
#   transformers modeling code, so no downgrade is required. transformers stays
#   unpinned (a transitive dep of sentence-transformers).
# IndicTransToolkit: IndicProcessor for normalization / language-tagging /
#   transliteration. sentencepiece: subword tokenisation for CTranslate2.
fasttext-wheel>=0.9.2
ctranslate2>=4.0
IndicTransToolkit>=1.0.0
sentencepiece>=0.1.99

# IL-TUR benchmark dataset (server/app/metrics/iltur_loader.py, tests/test_chatbot_iltur.py)
datasets>=2.19.0

flake8
